FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

ENV AETHERION_AGENT=PhysicistAgent

EXPOSE 8000

CMD ["uvicorn", "agents.services.agent_server:app", "--host", "0.0.0.0", "--port", "8000"]
